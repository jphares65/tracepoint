# TracePoint Armory Dark Theme Fix

This package keeps the Armory Live Data Bridge functionality, but restores the page to a dark TracePoint-style interface.

It replaces only:

- `src/app/firearms/page.tsx`

No SQL is required.
