export type ActiveDepartmentResult = { data: unknown; error: { message: string } | null }; export interface ActiveDepartmentReadDataSource { listMemberships(userId: string): PromiseLike<ActiveDepartmentResult>; } export class ActiveDepartmentReadAuthorizationError extends Error {} export class ActiveDepartmentReadRepositoryError extends Error {} export function requireActiveDepartmentReadProvider(value?: string) { const provider = value?.trim().toLowerCase() || "supabase"; if (provider !== "supabase") throw new Error(`Unsupported data provider: ${provider}. Only supabase is implemented.`); return provider; } export class UserBoundActiveDepartmentReadRepository { private readonly source: ActiveDepartmentReadDataSource; private readonly userId: string; constructor(source: ActiveDepartmentReadDataSource, userId: string) { if (!userId) throw new ActiveDepartmentReadAuthorizationError(); this.source = source; this.userId = userId; } async listMemberships(userId: string) { if (userId !== this.userId) throw new ActiveDepartmentReadAuthorizationError(); const result = await this.source.listMemberships(userId); if (result.error) throw new ActiveDepartmentReadRepositoryError(result.error.message); return Array.isArray(result.data) ? result.data as Record<string, unknown>[] : []; } }
