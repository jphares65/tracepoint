# TracePoint Clean Inspections Workflow

This package replaces the old `/inspections` page with a clean Armory workflow page.

It intentionally avoids lucide-react icons to prevent icon/import issues.

After copying the file, update the Armory card link from `#selected-firearm` to `/inspections`.
