# AWS release evidence ? September 5, 2026 UTC

TracePoint has operational AWS staging infrastructure and public pages, but authenticated application acceptance and production cutover remain blocked. Deadline: Wednesday, September 9, 2026. No production account, production data, production DNS, main deployment, or protected user file was modified.

## Release outcome

- Starting remote branch: 3c5e80250dbb37a1a6d62a48c90b54ee208b330a. Repaired Vercel deployment 4mbk2tQiS7bo5F1zGvjhXURDS35n reports success; GitHub deployment 6275743764 is Preview and production_environment=false. Main remains e33e4a4f17b662dddec9bf653ebf4051c3eb78ab.
- Use AWS_PROFILE=tracepoint-member-staging, AWS_REGION=us-east-1 and AWS_DEFAULT_REGION=us-east-1. STS account is 559054714699; role ARN is arn:aws:sts::559054714699:assumed-role/AWSReservedSSO_TracePointMigrationStaging_52cda9da92884a87/jason.phares. The similarly named tracepoint-staging profile points to forbidden management account 265544358665; it was not used.
- Requested image 3c5e802 was published by CodeBuild 4ccdaf2b-fbe5-4bc1-bfa2-cbec0d891634 from versioned source W4_RjuciHrMy90pfZ9y.qVJCgffcBBTP. Scan COMPLETE, zero findings. Digest sha256:a5a0afc42e555827ba5cc9ba90a237917de9db8bb4ac82c4301c40807fbe3172. **Do not deploy this image:** its compiled public key was malformed.
- Corrected image 7635a07c1c0cfb27f5611bc46ddedb5e9b9b8636 was published after restoring staging keys, by CodeBuild c635bd33-43b9-40e8-a8e7-fe56dd1f0189. Scan COMPLETE, zero findings. Digest sha256:c26c45c22832d419a885c015c5ddb7b4af0bd131ac19898f70d38142dd2c75c8. Subsequent changes affect scripts, SQL and evidence, not its application runtime source. No further image build is needed solely for these changes or a server-side Brevo key replacement.
- New runtime deployment was explicitly attempted through the guarded script and rejected before deployment: malformed staging Brevo key. Brevo read-only /v3/account returned 401. No email was sent.
- The live structural comparison allowed only the candidate image plus two alarms. A separately reviewed alarm-only assembly retained the old image and was deployed successfully, UPDATE_COMPLETE. It added application target 5xx and memory alarms without replacing ECS tasks.
- Final ECS desired/running/pending is 1/1/0, rollout completed, revision 7, healthy ALB target. Running image remains 6b0e3028f3e5e97d567de20c05637bb0cb64e7b7, digest sha256:2213aa8c082475108c17e123cc95f360356e1ddd48b9fb76053ce7aec9a11b28. Its scan is also COMPLETE with zero findings. ECS container health field is UNKNOWN; the verified health signal is ALB target health.
- Four alarms are OK; all have zero notification destinations. Last-30-minute ERROR/Exception/Unhandled event count is 0. Public login/health return 200 and all eight protected API checks redirect to login. Public smoke 10/10 passes.
- ACM certificate 90d7c1b4-3d71-4168-a908-8678501f5e5a is ISSUED in the staging account/region. Hostname remains https://staging.tracepointhq.com.
- Rollback was not executed: the new runtime is blocked, and only revision 7 is ACTIVE; revisions 1?6 are INACTIVE. The next successful deployment must preserve revision 7 for the explicit rehearsal and finish back on the corrected image. A clean prior scan alone is not a rollback rehearsal.

## Implemented repairs and validation

1. Provider gates now validate key shape/project/role and read-only Supabase public/admin endpoints. Full deployment and secret replacement also require a valid Brevo account probe. Image publication and independent auth fixtures can proceed while email is blocked. Seven focused tests pass; no response contents or keys are logged by these tools.
2. Secret replacement uses JSON over stdin to a Node wrapper with Windows-safe argument serialization, exact STS gates, remote provider probes and exact-version readback. A focused repair-staging-brevo.ps1 helper preserves the other seven fields and accepts the replacement through SecureString input. No email is sent by validation.
3. Supabase CLI access recovered project-specific keys for wztqqqashilusoppddxi. Modern secret keys returned by that project failed admin probes; the legacy service-role key passed. The stored staging publishable key and server-only legacy key were restored, probes and readback verified. No production project keys were retrieved. Current application-secret version is 71dda25f-f3ea-48e2-af96-c178624a6a11; Brevo is still malformed.
4. A parser error inspecting the malformed previous secret exposed two generated staging values in tool output. Stored NOTIFICATION_DISPATCH_SECRET and NEXT_SERVER_ACTIONS_ENCRYPTION_KEY were rotated and read back. Current revision 7 still uses its previous injected values until the corrected image is deployed; rotation is not fully effective in the running task yet. The corrected image was built using the rotated Server Actions key. No production secrets were involved.
5. Live staging lacked backend grants on 70 of 72 public tables. Migration 202609050001 grants the existing server-only role CRUD/sequence access; it does not alter BYPASSRLS, ownership or policies. Live verification now shows access across all 72 tables.
6. Live authenticated equipment SELECT failed because later migrations created RLS policies without grants. Migration 202609050002 grants only operations represented by existing authenticated/PUBLIC policies on RLS-enabled tables. No unprotected table receives new grants from that migration. Both migrations were applied transactionally to staging and recorded in its ledger; transaction assertions verified policy hashes were unchanged.
7. All 58 repository migration versions exactly match the live ledger. A fresh 58-migration local bootstrap passes. The local tenant test no longer adds broad test-only grants. Live testing with random staging fixtures and existing grants passes own-tenant reads, foreign read/write denial and non-manager write denial; the transaction rolls back all test records.
8. The reusable disposable acceptance runner creates a generated-password auth user and two staging departments, assigns a manager role, invokes supported browser login, and removes only its own fixtures. A fixtures-only execution completed with verified cleanup. Earlier cleanup failures exposed audited child cascade ordering; child rows are now deleted before departments. All abandoned fixture users/departments were subsequently verified removed. Full authenticated browser setup still fails on the old deployed bundle; no full workflow acceptance is claimed.
9. CloudFormation template retrieval now uses JSON instead of the AWS CLI text path, which failed live with a str/items error. Strict synthesis, structural validation and live diff review passed. Changed-file lint, PowerShell parsing and 15 existing deployment-safety cases pass. Previously completed broad tests/builds were not rerun without a relevant change.

Supabase remains database, authentication and object storage. Brevo remains selected email; SES is prepared but disabled. No AWS database, auth or object-storage provider replacement is claimed.

## Weighted completion

Before this continuation: 47.80%. After: 45.55%. The decrease withdraws deployed authentication/email configuration credit disproved by live tests; stored credential repairs do not establish a corrected running application. Actual cost verification adds its existing checklist credit. Documentation, fresh scans of already-counted publication capability and fixture scaffolding add no points. See aws-migration-checklist-20260905.json.

| Category | Weight | Complete | Evidence | Exact remaining work | Can Codex finish without user? |
|---|---:|---:|---|---|---|
| AWS account/governance | 6 | 50.00% | Exact member profile/STS account and role verified. | Production account authority and organization controls. | No: account owner |
| network and compute | 10 | 60.00% | Live ECS/ALB and issued ACM; strict alarm-only assembly deployed. | Production network, capacity and failure validation. | After production access |
| runtime deployment | 8 | 25.00% | Revision 7 healthy; two new immutable images scanned; corrected candidate ready. | Valid Brevo key, corrected runtime deployment, full acceptance and rollback rehearsal. | After staging secret repair |
| database/schema/data migration | 16 | 50.00% | 58 migrations match ledger; clean bootstrap and live RLS negatives pass; policy hashes unchanged. | Live data manifest reconciliation and separately authorized production data/PITR work. | Staging verification yes; production no |
| authentication | 10 | 20.00% | Stored staging public/admin keys recovered and probed; fixture auth user creation/deletion verified. | Deploy corrected bundle, password login/session/role workflow acceptance; provider replacement. | After staging deployment gate |
| storage | 8 | 50.00% | Existing tested provider interface and checksum tooling retained. | Live object transfer/access acceptance; private S3 delivery implementation. | Staging fixtures possible after auth |
| email | 6 | 25.00% | Brevo read-only probe is 401; prepared SES adapter remains disabled. | Valid staging Brevo key; delivery/suppression verification; production SES activation separately. | No: required provider credential |
| DNS/TLS | 8 | 50.00% | Staging issued ACM and public HTTPS pass. Main production untouched. | Production certificate and approved DNS/traffic cutover. | No: production authority |
| CI/CD | 8 | 60.00% | Two commit-addressed CodeBuild publications and clean scans; provider gates tested; Git push works. | OIDC release execution and full deploy/recovery chain. | After required secrets/environment setup |
| security/monitoring/backups | 10 | 62.50% | Four deployed alarms OK; logs error count 0; measured MTD below budget. | Alarm recipients/escalation, production restore, WAF/load validation. | Partly; recipients/production access needed |
| production account readiness | 6 | 50.00% | Existing offline preview and separation tests preserved. Only two local AWS profiles; none production. | Dedicated production account, authorized role, live safety gate. | No: account owner |
| cutover/rollback validation | 4 | 25.00% | Existing executable input gates; old image scan clean. | Only rev7 ACTIVE; previous revisions inactive. Full runtime rollback and DNS rehearsal remain. | Runtime after release; DNS requires authority |

## Cost evidence

Cost Explorer estimated unblended charges for September 1 through September 4 total $0.9478775816; AWS Budgets reports $0.948 actual against $75. These values are billing-lagged month-to-date data, not a full-month guarantee. Forecast API reports insufficient history. The retained full-month design model is $54.17 and already includes four alarms. Two CodeBuild jobs and two standard alarms were the added chargeable work; no NAT, extra service tasks, WAF or stateful infrastructure was introduced. See aws-cost-evidence-20260905.json.

## Jason's shortest manual action and the next run

1. Obtain the authorized **staging** Brevo API key. In the isolated branch checkout, set AWS_PROFILE to tracepoint-member-staging and run ./scripts/repair-staging-brevo.ps1. Paste through the terminal paste command into the masked prompt. Do not paste secrets into chat, edit API KEYS.txt, or replace the repaired Supabase and generated fields. The helper rejects an invalid key before writing.
2. Codex can then deploy the already-built corrected 7635a07 image through the existing strict gate; no public build variable changed when only Brevo is repaired. Wait for stable 1/1 and target health, run disposable authenticated acceptance and complete remaining workflow fixtures, inspect logs/alarms, rehearse rollback and return to the corrected task. Do not promote or deploy the malformed 3c5e802 image.
3. Provide the dedicated production workload account/role, alarm escalation recipient and cutover/rollback owner. Production credentials, data transfer and DNS cutover remain separate explicit gates. Existing production preview engineering remains available; no default/management profile may be used.

The next autonomous run should start at the provider gate and corrected-image deployment, then finish authenticated workflows and rollback. Do not repeat inventory, dependency analysis or the already-proven bootstrap unless new migrations change it. Production hosting cutover can initially retain Supabase and Brevo, but provider exit is still a separate migration. TracePoint is not production cutover-ready or fully migrated.
