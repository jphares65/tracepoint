import test from 'node:test';
import assert from 'node:assert/strict';
import {validateRequestControlsDeployment} from './validate-request-controls-deployment.mjs';
test('only intended WAF rule transitions can update existing resources',()=>{const t={Resources:{acl:{Type:'AWS::WAFv2::WebACL',Properties:{Name:'tracepoint-staging-requests',Scope:'REGIONAL',DefaultAction:{Allow:{}},VisibilityConfig:{SampledRequestsEnabled:false},Rules:[]}},association:{Type:'AWS::WAFv2::WebACLAssociation',Properties:{ResourceArn:'arn:aws:elasticloadbalancing:us-east-1:559054714699:loadbalancer/app/tracep-Servi-G9c0RkjQMCj4/af079dabc04bbb9c'}}}};validateRequestControlsDeployment(t);const next=structuredClone(t);next.Resources.acl.Properties.Rules=[{Name:'count'}];validateRequestControlsDeployment(next,t);const bad=structuredClone(next);bad.Resources.acl.Properties.DefaultAction={Block:{}};assert.throws(()=>validateRequestControlsDeployment(bad,t));assert.throws(()=>validateRequestControlsDeployment({Resources:{...t.Resources,task:{Type:'AWS::ECS::TaskDefinition'}}},t));const moved=structuredClone(next);moved.Resources.association.Properties.ResourceArn=moved.Resources.association.Properties.ResourceArn.replace('559054714699','265544358665');assert.throws(()=>validateRequestControlsDeployment(moved,t));});
