import "server-only";import {requireEvidenceReadProvider,TenantBoundEvidenceReadRepository} from "./read-repository-core";import {SupabaseEvidenceReadDataSource,type Client}from"./read-repository-supabase";export function createEvidenceReadRepository(client:Client,d:string){requireEvidenceReadProvider(process.env.TRACEPOINT_DATA_PROVIDER);return new TenantBoundEvidenceReadRepository(new SupabaseEvidenceReadDataSource(client),d);}
