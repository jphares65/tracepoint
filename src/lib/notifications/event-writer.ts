import "server-only"; import { TenantBoundNotificationEventWriter } from "./event-writer-core"; import { SupabaseNotificationEventWriteDataSource, type NotificationWriteClient } from "./event-writer-supabase"; export function createNotificationEventWriter(client: NotificationWriteClient, departmentId: string, userId: string) { return new TenantBoundNotificationEventWriter(new SupabaseNotificationEventWriteDataSource(client), departmentId, userId); }
