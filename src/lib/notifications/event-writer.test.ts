import assert from "node:assert/strict"; import test from "node:test"; import { NotificationWriteAuthorizationError, NotificationWriteRepositoryError, TenantBoundNotificationEventWriter, type NotificationEventWriteDataSource } from "./event-writer-core.ts"; const source = (overrides: Partial<NotificationEventWriteDataSource> = {}): NotificationEventWriteDataSource => ({ upsertEvent: async () => ({ error: null }), upsertEmail: async () => ({ error: null }), resolveEvent: async () => ({ error: null }), ...overrides }); test("notification event writes require matching tenant and user", async () => { let calls = 0; const writer = new TenantBoundNotificationEventWriter(source({ upsertEvent: async () => { calls++; return { error: null }; } }), "d", "u"); await assert.rejects(writer.upsertEvent({ department_id: "x", user_id: "u" }), NotificationWriteAuthorizationError); await assert.rejects(writer.upsertEvent({ department_id: "d", user_id: "x" }), NotificationWriteAuthorizationError); assert.equal(calls, 0); await writer.upsertEvent({ department_id: "d", user_id: "u" }); assert.equal(calls, 1); }); test("notification event writes preserve provider errors", async () => { const writer = new TenantBoundNotificationEventWriter(source({ resolveEvent: async () => ({ error: { message: "failed" } }) }), "d", "u"); await assert.rejects(writer.resolveEvent("n", "now"), NotificationWriteRepositoryError); });
import { SupabaseNotificationEventWriteDataSource, type NotificationWriteClient } from "./event-writer-supabase.ts"; test("notification writer adapter preserves conflict and resolution contracts", async () => {const calls:string[]=[];let query:any;query=new Proxy({then:(resolve:any)=>resolve({error:null})},{get(target,key){if(key==="then")return target.then;return(...args:any[])=>{calls.push(`${String(key)}:${JSON.stringify(args)}`);return query;};}});const adapter=new SupabaseNotificationEventWriteDataSource({from:(table:string)=>{calls.push(`from:${table}`);return query;}} as NotificationWriteClient);await adapter.upsertEvent({id:"n"});await adapter.upsertEmail({id:"e"});await adapter.resolveEvent("n","now");assert.ok(calls.includes('upsert:[{"id":"n"},{"onConflict":"department_id,user_id,notification_key"}]'));assert.ok(calls.includes('upsert:[{"id":"e"},{"onConflict":"department_id,user_id,notification_key,fingerprint","ignoreDuplicates":true}]'));assert.ok(calls.includes('update:[{"resolved_at":"now","updated_at":"now"}]'));});
