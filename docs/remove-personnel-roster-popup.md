# Remove Personnel Roster Button Panel

This package removes the temporary visible Personnel Bridge panel / "Load Live Personnel to Roster" control from the Range Days workspace.

It preserves the underlying Pilot Personnel Bridge. The app will still use `/api/pilot/personnel` and live Supabase personnel where the bridge is already wired.
