import "server-only"; import { AuthorizedPlatformReadRepository, requirePlatformReadProvider } from "./read-repository-core"; import { SupabasePlatformReadDataSource, type PlatformClient } from "./read-repository-supabase"; export function createPlatformReadRepository(client: unknown, authorized: boolean) { requirePlatformReadProvider(process.env.TRACEPOINT_DATA_PROVIDER); return new AuthorizedPlatformReadRepository(new SupabasePlatformReadDataSource(client as PlatformClient), authorized); }
