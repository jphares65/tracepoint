import {execFileSync} from 'node:child_process';
import assert from 'node:assert/strict';
import {writeFileSync} from 'node:fs';
import {operationsEvidence} from './staging-operations-evidence.mjs';
const repository='jphares65/tracepoint',branch='codex/aws-staging-readiness-20260902';
try {
 const sha=process.argv[2];assert.match(sha??'',/^[0-9a-f]{40}$/);
 const git=args=>execFileSync('git',['-c','safe.directory='+process.cwd(),...args],{encoding:'utf8',stdio:['ignore','pipe','pipe']}).trim();
 assert.match(git(['remote','get-url','origin']),/^https:\/\/github\.com\/jphares65\/tracepoint(?:\.git)?$/);
 const credential=execFileSync('git',['-c','safe.directory='+process.cwd(),'credential','fill'],{input:'protocol=https\nhost=github.com\n\n',encoding:'utf8',stdio:['pipe','pipe','pipe'],env:{...process.env,GIT_TERMINAL_PROMPT:'0',GCM_INTERACTIVE:'never'}});
 const token=credential.split(/\r?\n/).find(x=>x.startsWith('password='))?.slice(9);assert.ok(token);
 async function get(path){const response=await fetch('https://api.github.com/repos/'+repository+path,{headers:{Authorization:'Bearer '+token,Accept:'application/vnd.github+json'},redirect:'error',signal:AbortSignal.timeout(20000)});assert.equal(response.status,200);return response.json();}
 const operations=process.argv.includes('--operations');
 const runs=await get('/actions/workflows/'+(operations?'aws-staging-operations.yml':'aws-staging-runtime.yml')+'/runs?'+new URLSearchParams({branch,head_sha:sha,per_page:'20'}));
 const run=runs.workflow_runs.find(x=>x.head_sha===sha&&x.head_branch===branch);
 const preview=(await get('/commits/'+sha+'/status')).statuses.find(x=>x.context==='Vercel');
 if(!run){console.log(JSON.stringify({commit:sha,workflowFound:false,preview:preview?.state??'pending'}));}
 else {
  const jobs=await get('/actions/runs/'+run.id+'/jobs?per_page=100');
  const report={commit:sha,workflowFound:true,run:run.id,attempt:run.run_attempt,status:run.status,conclusion:run.conclusion,url:run.html_url,preview:preview?.state??'pending',checkedAtUTC:new Date().toISOString(),jobs:jobs.jobs.map(job=>({id:job.id,name:job.name,status:job.status,conclusion:job.conclusion,steps:job.steps.map(step=>({name:step.name,status:step.status,conclusion:step.conclusion}))}))};
  if(process.argv.includes('--completed-logs')){
   assert.equal(run.status,'completed');report.execution=[];
   for(const job of jobs.jobs){
    let response=await fetch('https://api.github.com/repos/'+repository+'/actions/jobs/'+job.id+'/logs',{headers:{Authorization:'Bearer '+token},redirect:'manual',signal:AbortSignal.timeout(20000)});
    if(response.status===302){const url=new URL(response.headers.get('location'));assert.equal(url.protocol,'https:');assert.ok(url.hostname.endsWith('.blob.core.windows.net')||url.hostname.endsWith('.actions.githubusercontent.com'));response=await fetch(url,{redirect:'error',signal:AbortSignal.timeout(20000)});}
    assert.equal(response.status,200);const logs=(await response.text()).split(/\r?\n/).map(line=>line.replace(/^\d{4}-\d\d-\d\dT\S+\s*/,'').replace(/\x1b\[[0-9;]*m/g,'')).join('\n');
    const evidence={job:job.name,testCounts:[...logs.matchAll(/(?:#|\u2139) tests (\d+)/g)].map(x=>Number(x[1])),fixtures:[],acceptanceFailures:[],recoveryFailures:[],deliveries:[],brevoPrerequisites:[],productionAccountReadiness:null,failureMarkers:{acceptanceHarness:logs.includes('Disposable staging setup or execution failed; sensitive details suppressed.'),brevo:logs.includes('Staging delivery verification failed; sensitive details suppressed.'),nodeGate:logs.includes('Staging Node gate failed after child cleanup completed.'),runtimeEvidence:logs.includes('Evidence collection failed; credentials and log contents suppressed.')},structuralGates:[],resourceDiff:[...new Set(logs.match(/\[(?:~|\+|-)\] AWS::[A-Za-z0-9:]+[^\r\n]*/g)??[])]};
    if(operations)evidence.operations=operationsEvidence(logs);
    for(const line of logs.split('\n')){try{const value=JSON.parse(line.trim());if(value.fixtureRun){const item={run:value.fixtureRun};for(const key of ['cleanup','storageCleanup','auditCreation','custodyHistory','documentAudit'])if(value[key]!==undefined)item[key]=value[key];if(Object.keys(item).length>1)evidence.fixtures.push(item);}if(typeof value.acceptanceStep==='string'&&Number.isInteger(value.status))evidence.acceptanceFailures.push({step:value.acceptanceStep,status:value.status,failureClass:value.failureClass,schemaObject:value.schemaObject,structuralDiagnostic:value.structuralDiagnostic});if(typeof value.step==='string'&&['unknown','400','401','403','404','409','422','500'].includes(String(value.code)))evidence.acceptanceFailures.push({step:value.step,code:String(value.code)});if(typeof value.browserRecoveryStep==='string')evidence.recoveryFailures.push({stage:value.browserRecoveryStep,networkCode:value.networkCode,errorName:value.errorName,errorClass:value.errorClass});if(value.acceptanceHarness==='failed'&&typeof value.stage==='string')evidence.recoveryFailures.push({stage:value.stage,errorClass:'HARNESS_FAILURE'});if(value.submitted===true&&value.recipient==='verified Brevo account owner')evidence.deliveries.push({run:value.run,messageId:value.messageId,submitted:true,recipient:value.recipient,taskRevision:value.taskRevision});if(['pending','verified'].includes(value.delivery))evidence.deliveries.push({run:value.run,messageId:value.messageId,delivery:value.delivery,events:Array.isArray(value.events)?value.events.map(x=>({event:x.event,date:x.date})):[]});if(value.brevoPrerequisites==='verified')evidence.brevoPrerequisites.push({status:'verified',taskRevision:value.taskRevision,sender:value.sender,recipient:value.recipient});if(value.brevoDelivery==='failed'&&['identity','secret','task-configuration','account-metadata','sender-metadata','submission','delivery-events'].includes(value.stage))evidence.brevoPrerequisites.push({status:'failed',stage:value.stage,submitted:value.submitted===true,sensitiveDetailsPrinted:value.sensitiveDetailsPrinted===true});if(value.safe===true&&typeof value.scope==='string')evidence.structuralGates.push({safe:true,scope:value.scope});}catch{}}
    const lines=logs.split('\n');
    for(let i=0;i<lines.length;i++){if(lines[i].trim()!=='{')continue;for(let j=i+1;j<Math.min(i+600,lines.length);j++){if(lines[j].trim()!=='}')continue;let value;try{value=JSON.parse(lines.slice(i,j+1).join('\n'));}catch{continue;}if(value.target==='https://staging.tracepointhq.com'&&Array.isArray(value.results)){for(const result of value.results.filter(x=>x.status==='fail'))evidence.acceptanceFailures.push({name:String(result.name).slice(0,120),status:'fail',diagnostic:{code:result.diagnostic?.code,actual:result.diagnostic?.actual,expected:result.diagnostic?.expected,step:result.diagnostic?.step}});evidence.acceptanceSummary={passed:value.results.filter(x=>x.status==='pass').length,failed:value.results.filter(x=>x.status==='fail').length,blocked:value.results.filter(x=>x.status==='blocked').length};}if(value.productionAccountReadiness){const source=value.productionAccountReadiness;evidence.productionAccountReadiness={};for(const key of ['checkedFromAccount','checkedRegion','organizationMetadataVisible','accountInventoryAuthorized','dedicatedProductionAccountExists','candidateCount','productionAccount','guardrailInspectionAuthorized','attachedScpCount','managementAccountUsedAsRuntime','mutationsPerformed'])evidence.productionAccountReadiness[key]=source[key]??null;}break;}}
    const rollback=logs.match(/\{\s*"currentTaskDefinition"\s*:[\s\S]*?\n\}/);
    if(rollback){const value=JSON.parse(rollback[0]);evidence.rollback={};for(const key of ['currentTaskDefinition','priorTaskDefinition','imageTag','rollbackVerified','rollbackSeconds','returnSeconds','correctedRuntimeRestored'])evidence.rollback[key]=value[key];}
    report.execution.push(evidence);
   }
  }
  if(process.argv.includes('--save'))writeFileSync('docs/aws-'+(operations?'operations-':'workflow-')+sha+'.json',JSON.stringify(report,null,2)+'\n');
  console.log(JSON.stringify(report,null,2));
 }
} catch {console.error('Staging workflow evidence unavailable; credentials and response details suppressed.');process.exitCode=1;}
